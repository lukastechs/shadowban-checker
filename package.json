{
  "name": "shadowban-checker",
  "version": "1.0.0",
  "description": "X/Twitter shadow ban checker API",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm install && npm run install-chrome",
    "install-chrome": "mkdir -p .cache/puppeteer && npx puppeteer browsers install chrome --path .cache/puppeteer"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "author": "Your Name",
  "license": "MIT"
}
