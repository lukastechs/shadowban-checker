services:
  - type: web
    name: shadowban-checker
    env: docker
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: X_BEARER_TOKEN
        sync: false
      - key: RECAPTCHA_SECRET_KEY
        sync: false
